// components/logo/classes.ts
import { createClasses } from '../../../theme/create-classes';

export const logoClasses = {
  root: createClasses('logo-root'),
  image: createClasses('logo-image'),
  text: createClasses('logo-text'),
};

